import styled from "styled-components";

export const BlogProvider = styled.div `
  padding: 4vh 7vw;
  box-sizing: border-box;
  margin: 0 auto;
  @media (--media-min-small) {
    padding: 2em;
  }
`;